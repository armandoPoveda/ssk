{% extends 'layout.html.twig' %}

{% block title %}CourseType
{% endblock %}

{% block content %}
	<div class="container" style="margin-top:1%; margin-bottom:1%;padding:10px;">
		<h1>CourseType</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ course_type.id }}</td>
				</tr>
				<tr>
					<th>Name</th>
					<td>{{ course_type.name }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('course_type_index') }}">back to list</a><br>

		<a href="{{ path('course_type_edit', {'id': course_type.id}) }}">edit</a>

		{{ include('course_type/_delete_form.html.twig') }}
	</div>
{% endblock %}
