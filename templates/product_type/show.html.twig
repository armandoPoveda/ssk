{% extends 'layout.html.twig' %}

{% block title %}ProductType
{% endblock %}

{% block content %}
	<div class="container" style="margin-top:1%; margin-bottom:1%;padding:10px;">
		<h1>ProductType</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ product_type.id }}</td>
				</tr>
				<tr>
					<th>Name</th>
					<td>{{ product_type.name }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('product_type_index') }}">back to list</a><br>

		<a href="{{ path('product_type_edit', {'id': product_type.id}) }}">edit</a>

		{{ include('product_type/_delete_form.html.twig') }}
	</div>
{% endblock %}
